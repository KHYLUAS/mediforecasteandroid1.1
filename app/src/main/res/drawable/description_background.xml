<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#f4f4f4"/> <!-- Background color (light gray) -->
    <corners android:radius="10dp"/> <!-- Rounded corners -->
    <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp"/> <!-- Padding around the text -->
    <stroke
        android:color="#dcdcdc"
        android:width="1dp"/> <!-- Optional border around the description -->
</shape>
